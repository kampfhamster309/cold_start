# Go
/backend/server
/backend/*.exe
/backend/**/*.test

# Node / Next.js: frontend/.gitignore (create-next-app's own) covers
# node_modules, .next, env files etc. in more detail than duplicating here.

# OS / editor
.DS_Store
*.swp

# Local env overrides
.env
.env.local

# Claude Code project instructions (local-only)
CLAUDE.md
